{
    "name": "dog_did",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "db:start": "docker-compose up -d",
        "db:apply": "atlas schema apply --env local",
        "db:apply:dry-run": "npm run db:apply -- --dry-run",
        "db:apply-generate": "npm run db:apply && npm run db:generate-types",
        "db:generate-types": "kysely-codegen --out-file src/db/types.ts --url mysql://dogdid:password@localhost:3306/dogdid && node db_types_fix.mjs",
        "start:watch": "ts-node-dev src/server.ts",
        "start": "DATABASE_URL=mysql://dogdid:password@localhost:3306/dogdid npm run start:watch"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@fastify/swagger": "^8.5.1",
        "@fastify/swagger-ui": "^1.8.1",
        "@paralleldrive/cuid2": "^2.2.1",
        "@types/bcrypt": "^5.0.0",
        "@types/node": "^20.2.5",
        "bcrypt": "^5.1.0",
        "esbuild": "^0.17.19",
        "fastify": "^4.17.0",
        "fastify-swagger": "^5.2.0",
        "fastify-zod": "^1.3.2",
        "kysely": "^0.26.1",
        "mongodb": "^5.5.0",
        "mysql2": "^3.5.2",
        "nodemon": "^2.0.22",
        "pino-pretty": "^10.0.0",
        "ts-node": "^10.9.1",
        "ts-node-dev": "^2.0.0",
        "tsc": "^2.0.4",
        "zod": "^3.21.4"
    },
    "devDependencies": {
        "kysely-codegen": "^0.10.1"
    }
}
